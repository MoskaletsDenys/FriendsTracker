@model FriendsTracker.Models.Friend
@{
    ViewBag.Title = Model.Name;
}
<h2>User @Model.Name</h2>
<div>
    <dl class="dl-horizontal">
        <dt>Id</dt>
        <dd>@Model.Id</dd>
        <dt>Name</dt>
        <dd>@Model.Name</dd>
        <dt>Age</dt>
        <dd>@Model.Age</dd>
    </dl>
</div>